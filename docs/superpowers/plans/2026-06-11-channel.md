# Channel 模块实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建独立的远程控制模块，支持通过微信渠道与 AI Agent 交互

**Architecture:** Consumer-Dispatch 模式，消息并发接收后入队，Agent 顺序处理并支持多次回复

**Tech Stack:** Python 3.10+, asyncio, aiohttp, dataclasses

---

## 文件结构

```
coloop-agent-channel/
├── pyproject.toml
├── config.yaml
├── src/
│   └── channel/
│       ├── __init__.py
│       ├── core/
│       │   ├── __init__.py
│       │   ├── message.py          # 消息模型
│       │   ├── queue.py            # 消息队列
│       │   └── session.py          # 会话管理
│       ├── gateway/
│       │   ├── __init__.py
│       │   ├── platform.py         # 平台接口
│       │   ├── registry.py         # 平台注册表
│       │   ├── consumer.py         # 消息消费者
│       │   └── dispatch.py         # 消息分发器
│       ├── platform/
│       │   ├── __init__.py
│       │   └── wechat/
│       │       ├── __init__.py
│       │       ├── ilink.py        # iLink 客户端
│       │       └── wechat.py       # 微信平台实现
│       ├── agent/
│       │   ├── __init__.py
│       │   ├── strategy.py         # Agent 接口
│       │   └── test.py             # 测试策略
│       └── cli/
│           ├── __init__.py
│           └── app.py              # CLI 入口
├── tests/
│   ├── __init__.py
│   ├── test_message.py
│   ├── test_queue.py
│   ├── test_session.py
│   └── test_agent.py
└── docs/
```

---

## Task 1: 项目初始化

**Files:**
- Create: `coloop-agent-channel/pyproject.toml`
- Create: `coloop-agent-channel/src/channel/__init__.py`
- Create: `coloop-agent-channel/tests/__init__.py`

- [ ] **Step 1: 创建项目目录**

```bash
mkdir -p coloop-agent-channel/src/channel
mkdir -p coloop-agent-channel/tests
```

- [ ] **Step 2: 创建 pyproject.toml**

```toml
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "coloop-agent-channel"
version = "0.1.0"
description = "远程控制模块，支持多渠道与 AI Agent 交互"
requires-python = ">=3.10"
dependencies = [
    "aiohttp>=3.9.0",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
```

- [ ] **Step 3: 创建 __init__.py**

```python
# src/channel/__init__.py
"""Channel - 远程控制模块"""
__version__ = "0.1.0"
```

```python
# tests/__init__.py
```

- [ ] **Step 4: 安装依赖并验证**

```bash
cd coloop-agent-channel
pip install -e ".[dev]"
python -c "import channel; print(channel.__version__)"
```

Expected: 输出 `0.1.0`

- [ ] **Step 5: 提交**

```bash
git add coloop-agent-channel/
git commit -m "feat(channel): 初始化项目结构"
```

---

## Task 2: 消息模型（Message）

**Files:**
- Create: `coloop-agent-channel/src/channel/core/__init__.py`
- Create: `coloop-agent-channel/src/channel/core/message.py`
- Create: `coloop-agent-channel/tests/test_message.py`

- [ ] **Step 1: 编写失败的测试**

```python
# tests/test_message.py
import pytest
from channel.core.message import Message


def test_create_message():
    """测试创建消息"""
    msg = Message(
        msg_id="msg_001",
        platform="wechat",
        user_id="user_123",
        chat_id="chat_456",
        content="Hello"
    )
    assert msg.msg_id == "msg_001"
    assert msg.platform == "wechat"
    assert msg.user_id == "user_123"
    assert msg.chat_id == "chat_456"
    assert msg.content == "Hello"
    assert msg.msg_type == "text"


def test_message_defaults():
    """测试消息默认值"""
    msg = Message(
        msg_id="msg_002",
        platform="wechat",
        user_id="user_123",
        chat_id="chat_456",
        content="Test"
    )
    assert msg.msg_type == "text"
    assert msg.media_url is None
    assert msg.media_key is None
    assert msg.raw is None
    assert msg.timestamp > 0


def test_message_with_media():
    """测试带媒体的消息"""
    msg = Message(
        msg_id="msg_003",
        platform="wechat",
        user_id="user_123",
        chat_id="chat_456",
        content="",
        msg_type="image",
        media_url="https://example.com/image.jpg",
        media_key="aes_key_123"
    )
    assert msg.msg_type == "image"
    assert msg.media_url == "https://example.com/image.jpg"
    assert msg.media_key == "aes_key_123"
```

- [ ] **Step 2: 运行测试验证失败**

```bash
cd coloop-agent-channel
pytest tests/test_message.py -v
```

Expected: FAIL - ModuleNotFoundError: No module named 'channel.core.message'

- [ ] **Step 3: 实现消息模型**

```python
# src/channel/core/__init__.py
```

```python
# src/channel/core/message.py
"""消息模型"""

from dataclasses import dataclass, field
from typing import Optional, Dict, Any
import time


@dataclass
class Message:
    """统一消息模型
    
    跨平台使用的消息格式，支持文本、语音、图片等类型。
    """
    
    # 基础信息
    msg_id: str                      # 消息 ID
    platform: str                    # 平台标识（wechat, telegram, etc.）
    user_id: str                     # 用户 ID
    chat_id: str                     # 会话 ID
    
    # 消息内容
    content: str                     # 文本内容
    msg_type: str = "text"           # 消息类型（text, voice, image, file）
    
    # 媒体信息（可选）
    media_url: Optional[str] = None  # 媒体 URL
    media_key: Optional[str] = None  # 媒体密钥（如 AES 密钥）
    
    # 元数据
    timestamp: float = field(default_factory=time.time)
    raw: Optional[Dict[str, Any]] = None  # 原始数据
```

- [ ] **Step 4: 运行测试验证通过**

```bash
cd coloop-agent-channel
pytest tests/test_message.py -v
```

Expected: 3 passed

- [ ] **Step 5: 提交**

```bash
git add coloop-agent-channel/src/channel/core/ coloop-agent-channel/tests/test_message.py
git commit -m "feat(channel): 添加消息模型 Message"
```

---

## Task 3: 消息队列（MessageQueue）

**Files:**
- Create: `coloop-agent-channel/src/channel/core/queue.py`
- Create: `coloop-agent-channel/tests/test_queue.py`

- [ ] **Step 1: 编写失败的测试**

```python
# tests/test_queue.py
import pytest
import asyncio
from channel.core.message import Message
from channel.core.queue import MessageQueue


@pytest.mark.asyncio
async def test_put_and_get():
    """测试放入和获取消息"""
    queue = MessageQueue()
    msg = Message(
        msg_id="msg_001",
        platform="wechat",
        user_id="user_123",
        chat_id="chat_456",
        content="Hello"
    )
    
    await queue.put(msg)
    assert not queue.empty
    
    result = await queue.get()
    assert result.msg_id == "msg_001"
    assert queue.empty


@pytest.mark.asyncio
async def test_get_if_any_with_message():
    """测试非阻塞获取（有消息）"""
    queue = MessageQueue()
    msg = Message(
        msg_id="msg_002",
        platform="wechat",
        user_id="user_123",
        chat_id="chat_456",
        content="Test"
    )
    
    await queue.put(msg)
    result = await queue.get_if_any()
    assert result is not None
    assert result.msg_id == "msg_002"


@pytest.mark.asyncio
async def test_get_if_any_empty():
    """测试非阻塞获取（空队列）"""
    queue = MessageQueue()
    result = await queue.get_if_any()
    assert result is None


@pytest.mark.asyncio
async def test_fifo_order():
    """测试先进先出顺序"""
    queue = MessageQueue()
    
    for i in range(3):
        msg = Message(
            msg_id=f"msg_{i:03d}",
            platform="wechat",
            user_id="user_123",
            chat_id="chat_456",
            content=f"Message {i}"
        )
        await queue.put(msg)
    
    for i in range(3):
        result = await queue.get()
        assert result.msg_id == f"msg_{i:03d}"
```

- [ ] **Step 2: 运行测试验证失败**

```bash
cd coloop-agent-channel
pytest tests/test_queue.py -v
```

Expected: FAIL - ModuleNotFoundError: No module named 'channel.core.queue'

- [ ] **Step 3: 实现消息队列**

```python
# src/channel/core/queue.py
"""消息队列"""

import asyncio
from typing import Optional
from .message import Message


class MessageQueue:
    """异步消息队列
    
    特点：
    - 并发安全：多个生产者可同时 put
    - 顺序处理：消费者按 FIFO 顺序 get
    - 非阻塞获取：get_if_any 立即返回
    """
    
    def __init__(self):
        self._queue: asyncio.Queue[Message] = asyncio.Queue()
    
    async def put(self, message: Message) -> None:
        """放入消息"""
        await self._queue.put(message)
    
    async def get(self) -> Message:
        """获取消息（阻塞等待）"""
        return await self._queue.get()
    
    async def get_if_any(self) -> Optional[Message]:
        """非阻塞获取消息"""
        try:
            return self._queue.get_nowait()
        except asyncio.QueueEmpty:
            return None
    
    @property
    def empty(self) -> bool:
        """队列是否为空"""
        return self._queue.empty()
```

- [ ] **Step 4: 运行测试验证通过**

```bash
cd coloop-agent-channel
pytest tests/test_queue.py -v
```

Expected: 4 passed

- [ ] **Step 5: 提交**

```bash
git add coloop-agent-channel/src/channel/core/queue.py coloop-agent-channel/tests/test_queue.py
git commit -m "feat(channel): 添加消息队列 MessageQueue"
```

---

## Task 4: Agent 策略接口（AgentStrategy）

**Files:**
- Create: `coloop-agent-channel/src/channel/agent/__init__.py`
- Create: `coloop-agent-channel/src/channel/agent/strategy.py`
- Create: `coloop-agent-channel/src/channel/agent/test.py`
- Create: `coloop-agent-channel/tests/test_agent.py`

- [ ] **Step 1: 编写失败的测试**

```python
# tests/test_agent.py
import pytest
from channel.agent.strategy import AgentStrategy
from channel.agent.test import TestStrategy


def test_agent_strategy_is_abstract():
    """测试 AgentStrategy 是抽象类"""
    with pytest.raises(TypeError):
        AgentStrategy()


@pytest.mark.asyncio
async def test_test_strategy():
    """测试 TestStrategy"""
    agent = TestStrategy(delay=0.01)
    responses = []
    
    async for reply in agent.stream("Hello"):
        responses.append(reply)
    
    assert len(responses) > 0
    assert any("Hello" in r for r in responses)


@pytest.mark.asyncio
async def test_test_strategy_multiple_replies():
    """测试 TestStrategy 多次回复"""
    agent = TestStrategy(delay=0.01)
    responses = []
    
    async for reply in agent.stream("Test"):
        responses.append(reply)
    
    # 应该有多条回复（思考、处理、结果）
    assert len(responses) >= 3
```

- [ ] **Step 2: 运行测试验证失败**

```bash
cd coloop-agent-channel
pytest tests/test_agent.py -v
```

Expected: FAIL - ModuleNotFoundError: No module named 'channel.agent'

- [ ] **Step 3: 实现 Agent 策略**

```python
# src/channel/agent/__init__.py
```

```python
# src/channel/agent/strategy.py
"""Agent 策略接口"""

from abc import ABC, abstractmethod
from typing import AsyncGenerator


class AgentStrategy(ABC):
    """Agent 策略抽象接口
    
    实现此接口即可接入不同的 AI Agent：
    - TestStrategy：测试用，返回模拟数据
    - ColoopStrategy：接入 coloop-agent
    - ShellStrategy：通过 shell 调用其他软件
    """
    
    @abstractmethod
    async def stream(self, message: str) -> AsyncGenerator[str, None]:
        """流式处理消息
        
        Args:
            message: 用户输入
            
        Yields:
            多个回复（思考过程、工具调用、最终结果等）
        """
        ...
        yield  # 使函数成为 generator
```

```python
# src/channel/agent/test.py
"""测试 Agent 策略"""

import asyncio
from typing import AsyncGenerator
from .strategy import AgentStrategy


class TestStrategy(AgentStrategy):
    """测试用策略，返回模拟数据"""
    
    def __init__(self, delay: float = 1.0):
        self.delay = delay
        self.call_count = 0
    
    async def stream(self, message: str) -> AsyncGenerator[str, None]:
        self.call_count += 1
        
        yield f"🤔 收到消息: {message}"
        await asyncio.sleep(self.delay)
        
        yield f"🔧 正在处理..."
        await asyncio.sleep(self.delay)
        
        yield f"✅ 处理完成！您说了: {message}"
```

- [ ] **Step 4: 运行测试验证通过**

```bash
cd coloop-agent-channel
pytest tests/test_agent.py -v
```

Expected: 3 passed

- [ ] **Step 5: 提交**

```bash
git add coloop-agent-channel/src/channel/agent/ coloop-agent-channel/tests/test_agent.py
git commit -m "feat(channel): 添加 Agent 策略接口和测试实现"
```

---

## Task 5: 会话管理（Session）

**Files:**
- Create: `coloop-agent-channel/src/channel/core/session.py`
- Create: `coloop-agent-channel/tests/test_session.py`

- [ ] **Step 1: 编写失败的测试**

```python
# tests/test_session.py
import pytest
import asyncio
from unittest.mock import AsyncMock
from channel.core.message import Message
from channel.core.session import Session
from channel.agent.test import TestStrategy


@pytest.fixture
def mock_sender():
    """模拟消息发送器"""
    return AsyncMock()


@pytest.fixture
def test_agent():
    """测试 Agent"""
    return TestStrategy(delay=0.01)


@pytest.mark.asyncio
async def test_session_process_single_message(mock_sender, test_agent):
    """测试处理单条消息"""
    session = Session(
        chat_id="chat_001",
        agent=test_agent,
        send_message=mock_sender
    )
    
    msg = Message(
        msg_id="msg_001",
        platform="wechat",
        user_id="user_123",
        chat_id="chat_001",
        content="Hello"
    )
    
    await session.on_message(msg)
    
    # 应该发送了多条回复
    assert mock_sender.call_count >= 3


@pytest.mark.asyncio
async def test_session_queue_message_while_processing(mock_sender, test_agent):
    """测试处理中消息入队"""
    session = Session(
        chat_id="chat_001",
        agent=test_agent,
        send_message=mock_sender
    )
    
    # 模拟正在处理
    session.is_processing = True
    
    msg = Message(
        msg_id="msg_002",
        platform="wechat",
        user_id="user_123",
        chat_id="chat_001",
        content="Queued"
    )
    
    await session.on_message(msg)
    
    # 消息应该入队了
    assert not session.queue.empty


@pytest.mark.asyncio
async def test_session_stop(mock_sender, test_agent):
    """测试停止处理"""
    session = Session(
        chat_id="chat_001",
        agent=test_agent,
        send_message=mock_sender
    )
    
    session.is_processing = True
    session.stop()
    
    assert session._stop is True
```

- [ ] **Step 2: 运行测试验证失败**

```bash
cd coloop-agent-channel
pytest tests/test_session.py -v
```

Expected: FAIL - ModuleNotFoundError: No module named 'channel.core.session'

- [ ] **Step 3: 实现会话管理**

```python
# src/channel/core/session.py
"""会话管理"""

import asyncio
import logging
from typing import Callable, Awaitable
from .message import Message
from .queue import MessageQueue
from ..agent.strategy import AgentStrategy

logger = logging.getLogger(__name__)


class Session:
    """用户会话
    
    核心逻辑：
    - is_processing 时：消息入队等待
    - 非处理时：调用 agent.stream()，支持多次回复
    - 一轮结束后：queue.get_if_any() 取队列第一条继续
    """
    
    def __init__(
        self,
        chat_id: str,
        agent: AgentStrategy,
        send_message: Callable[[str, str], Awaitable[None]],
    ):
        self.chat_id = chat_id
        self.agent = agent
        self.send_message = send_message
        
        self.queue = MessageQueue()
        self.is_processing = False
        self._stop = False
    
    async def on_message(self, message: Message) -> None:
        """收到消息时调用"""
        if self.is_processing:
            logger.info(f"Session {self.chat_id}: 正在处理，消息入队")
            await self.queue.put(message)
            await self.send_message(self.chat_id, "收到，稍等...")
            return
        
        self.is_processing = True
        self._stop = False
        
        try:
            await self._run(message)
        except Exception as e:
            logger.error(f"Session {self.chat_id}: 处理出错: {e}")
            await self.send_message(self.chat_id, f"出错了: {e}")
        finally:
            self.is_processing = False
    
    async def _run(self, message: Message) -> None:
        """执行处理循环"""
        current = message
        
        while current and not self._stop:
            async for reply in self.agent.stream(current.content):
                if self._stop:
                    break
                await self.send_message(self.chat_id, reply)
            
            # 一轮结束，检查队列
            current = await self.queue.get_if_any()
            if current:
                logger.info(f"Session {self.chat_id}: 处理队列中的消息")
    
    def stop(self) -> None:
        """停止当前处理"""
        self._stop = True
```

- [ ] **Step 4: 运行测试验证通过**

```bash
cd coloop-agent-channel
pytest tests/test_session.py -v
```

Expected: 3 passed

- [ ] **Step 5: 提交**

```bash
git add coloop-agent-channel/src/channel/core/session.py coloop-agent-channel/tests/test_session.py
git commit -m "feat(channel): 添加会话管理 Session"
```

---

## Task 6: 平台接口（Platform）

**Files:**
- Create: `coloop-agent-channel/src/channel/gateway/__init__.py`
- Create: `coloop-agent-channel/src/channel/gateway/platform.py`
- Create: `coloop-agent-channel/src/channel/gateway/registry.py`

- [ ] **Step 1: 实现平台接口**

```python
# src/channel/gateway/__init__.py
```

```python
# src/channel/gateway/platform.py
"""平台抽象接口"""

from abc import ABC, abstractmethod
from typing import Callable, Awaitable, Optional
from ..core.message import Message


class Platform(ABC):
    """平台抽象接口
    
    所有平台实现（微信、Telegram 等）都需要实现此接口。
    """
    
    @abstractmethod
    async def connect(self) -> None:
        """连接到平台"""
        ...
    
    @abstractmethod
    async def disconnect(self) -> None:
        """断开连接"""
        ...
    
    @abstractmethod
    async def send_message(self, chat_id: str, content: str) -> bool:
        """发送消息"""
        ...
    
    @abstractmethod
    async def send_typing(self, chat_id: str) -> bool:
        """发送正在输入状态"""
        ...
    
    @abstractmethod
    def get_name(self) -> str:
        """获取平台名称"""
        ...
    
    @abstractmethod
    def is_connected(self) -> bool:
        """是否已连接"""
        ...
    
    def set_message_handler(self, handler: Callable[[Message], Awaitable[None]]) -> None:
        """设置消息处理器"""
        self._handler = handler
```

- [ ] **Step 2: 实现平台注册表**

```python
# src/channel/gateway/registry.py
"""平台注册表"""

import logging
from typing import Dict, Optional
from .platform import Platform

logger = logging.getLogger(__name__)


class PlatformRegistry:
    """平台注册表"""
    
    def __init__(self):
        self._platforms: Dict[str, Platform] = {}
    
    def register(self, platform: Platform) -> None:
        """注册平台"""
        name = platform.get_name()
        self._platforms[name] = platform
        logger.info(f"注册平台: {name}")
    
    def get(self, name: str) -> Optional[Platform]:
        """获取平台"""
        return self._platforms.get(name)
    
    def get_all(self) -> Dict[str, Platform]:
        """获取所有平台"""
        return self._platforms.copy()
    
    async def connect_all(self) -> None:
        """连接所有平台"""
        for name, platform in self._platforms.items():
            try:
                await platform.connect()
                logger.info(f"平台 {name} 已连接")
            except Exception as e:
                logger.error(f"平台 {name} 连接失败: {e}")
    
    async def disconnect_all(self) -> None:
        """断开所有平台"""
        for name, platform in self._platforms.items():
            try:
                await platform.disconnect()
                logger.info(f"平台 {name} 已断开")
            except Exception as e:
                logger.error(f"平台 {name} 断开失败: {e}")
```

- [ ] **Step 3: 提交**

```bash
git add coloop-agent-channel/src/channel/gateway/
git commit -m "feat(channel): 添加平台接口和注册表"
```

---

## Task 7: 消息消费者和分发器（Consumer & Dispatch）

**Files:**
- Create: `coloop-agent-channel/src/channel/gateway/consumer.py`
- Create: `coloop-agent-channel/src/channel/gateway/dispatch.py`

- [ ] **Step 1: 实现消息消费者**

```python
# src/channel/gateway/consumer.py
"""消息消费者"""

import asyncio
import logging
from typing import Callable, Awaitable
from ..core.message import Message
from ..core.queue import MessageQueue

logger = logging.getLogger(__name__)


class StreamConsumer:
    """消息消费者
    
    从平台接收消息，放入全局队列。
    """
    
    def __init__(self):
        self._queue = MessageQueue()
        self._running = False
    
    async def start(self) -> None:
        """启动消费者"""
        self._running = True
        logger.info("消息消费者已启动")
    
    async def stop(self) -> None:
        """停止消费者"""
        self._running = False
        logger.info("消息消费者已停止")
    
    async def on_message(self, message: Message) -> None:
        """接收消息"""
        if not self._running:
            logger.warning("消费者未运行，丢弃消息")
            return
        
        logger.info(f"收到消息: {message.msg_id} from {message.platform}")
        await self._queue.put(message)
    
    async def get_message(self) -> Message:
        """获取消息"""
        return await self._queue.get()
    
    async def get_message_if_any(self) -> Message | None:
        """非阻塞获取消息"""
        return await self._queue.get_if_any()
```

- [ ] **Step 2: 实现消息分发器**

```python
# src/channel/gateway/dispatch.py
"""消息分发器"""

import asyncio
import logging
from typing import Dict, Callable, Awaitable
from ..core.message import Message
from ..core.session import Session
from ..agent.strategy import AgentStrategy
from .registry import PlatformRegistry
from .consumer import StreamConsumer

logger = logging.getLogger(__name__)


class StreamDispatch:
    """消息分发器
    
    从消费者获取消息，路由到对应的 Session。
    """
    
    def __init__(self):
        self._sessions: Dict[str, Session] = {}
        self._running = False
        self._task = None
    
    async def start(
        self,
        agent_strategy: AgentStrategy,
        platform_registry: PlatformRegistry,
        consumer: StreamConsumer,
    ) -> None:
        """启动分发器"""
        self._agent = agent_strategy
        self._registry = platform_registry
        self._consumer = consumer
        self._running = True
        
        # 启动分发循环
        self._task = asyncio.create_task(self._dispatch_loop())
        logger.info("消息分发器已启动")
    
    async def stop(self) -> None:
        """停止分发器"""
        self._running = False
        if self._task:
            self._task.cancel()
            try:
                await self._task
            except asyncio.CancelledError:
                pass
        logger.info("消息分发器已停止")
    
    async def _dispatch_loop(self) -> None:
        """分发循环"""
        while self._running:
            try:
                message = await self._consumer.get_message()
                await self._dispatch(message)
            except asyncio.CancelledError:
                break
            except Exception as e:
                logger.error(f"分发消息出错: {e}")
    
    async def _dispatch(self, message: Message) -> None:
        """分发单条消息"""
        chat_id = message.chat_id
        
        # 获取或创建 Session
        if chat_id not in self._sessions:
            platform = self._registry.get(message.platform)
            if not platform:
                logger.error(f"未知平台: {message.platform}")
                return
            
            session = Session(
                chat_id=chat_id,
                agent=self._agent,
                send_message=platform.send_message,
            )
            self._sessions[chat_id] = session
        
        session = self._sessions[chat_id]
        await session.on_message(message)
```

- [ ] **Step 3: 提交**

```bash
git add coloop-agent-channel/src/channel/gateway/consumer.py coloop-agent-channel/src/channel/gateway/dispatch.py
git commit -m "feat(channel): 添加消息消费者和分发器"
```

---

## Task 8: 微信平台实现（WeChat Platform）

**Files:**
- Create: `coloop-agent-channel/src/channel/platform/__init__.py`
- Create: `coloop-agent-channel/src/channel/platform/wechat/__init__.py`
- Create: `coloop-agent-channel/src/channel/platform/wechat/ilink.py`
- Create: `coloop-agent-channel/src/channel/platform/wechat/wechat.py`

- [ ] **Step 1: 实现 iLink 客户端**

```python
# src/channel/platform/__init__.py
```

```python
# src/channel/platform/wechat/__init__.py
```

```python
# src/channel/platform/wechat/ilink.py
"""iLink API 客户端"""

import asyncio
import logging
from typing import Optional, Dict, Any, List

import aiohttp

logger = logging.getLogger(__name__)


class ILinkClient:
    """iLink API 客户端
    
    用于与微信 iLink API 交互。
    注意：API 路径为推测，实际使用时需验证。
    """
    
    def __init__(
        self,
        token: str,
        api_base: str = "https://ilinkai.weixin.qq.com",
    ):
        self.token = token
        self.api_base = api_base.rstrip("/")
        self._session: Optional[aiohttp.ClientSession] = None
        self._last_msg_id: Optional[str] = None
    
    async def _get_session(self) -> aiohttp.ClientSession:
        if self._session is None or self._session.closed:
            self._session = aiohttp.ClientSession(
                headers={"Authorization": f"Bearer {self.token}"}
            )
        return self._session
    
    async def close(self) -> None:
        if self._session and not self._session.closed:
            await self._session.close()
    
    async def verify(self) -> bool:
        """验证 token"""
        try:
            session = await self._get_session()
            async with session.get(f"{self.api_base}/api/verify") as resp:
                return resp.status == 200
        except Exception as e:
            logger.error(f"验证失败: {e}")
            return False
    
    async def get_messages(self) -> List[Dict[str, Any]]:
        """获取新消息"""
        try:
            session = await self._get_session()
            params = {}
            if self._last_msg_id:
                params["since"] = self._last_msg_id
            
            async with session.get(
                f"{self.api_base}/api/messages",
                params=params,
            ) as resp:
                if resp.status == 200:
                    data = await resp.json()
                    messages = data.get("messages", [])
                    if messages:
                        self._last_msg_id = messages[-1].get("msg_id")
                    return messages
                return []
        except Exception as e:
            logger.error(f"获取消息失败: {e}")
            return []
    
    async def send_message(self, to_user: str, content: str) -> bool:
        """发送消息"""
        try:
            session = await self._get_session()
            async with session.post(
                f"{self.api_base}/api/sendmessage",
                json={"to_user": to_user, "content": content},
            ) as resp:
                return resp.status == 200
        except Exception as e:
            logger.error(f"发送消息失败: {e}")
            return False
    
    async def send_typing(self, to_user: str) -> bool:
        """发送正在输入状态"""
        try:
            session = await self._get_session()
            async with session.post(
                f"{self.api_base}/api/typing",
                json={"to_user": to_user},
            ) as resp:
                return resp.status == 200
        except Exception as e:
            logger.error(f"发送 typing 失败: {e}")
            return False
```

- [ ] **Step 2: 实现微信平台**

```python
# src/channel/platform/wechat/wechat.py
"""微信平台实现"""

import asyncio
import logging
from typing import Callable, Awaitable, Optional
from ...gateway.platform import Platform
from ...core.message import Message
from .ilink import ILinkClient

logger = logging.getLogger(__name__)


class WeChatPlatform(Platform):
    """微信平台实现"""
    
    def __init__(self, token: str, api_base: str = "https://ilinkai.weixin.qq.com"):
        self.token = token
        self.api_base = api_base
        self._client: Optional[ILinkClient] = None
        self._handler: Optional[Callable[[Message], Awaitable[None]]] = None
        self._connected = False
        self._poll_task: Optional[asyncio.Task] = None
    
    async def connect(self) -> None:
        self._client = ILinkClient(self.token, self.api_base)
        
        if not await self._client.verify():
            raise Exception("iLink token 无效")
        
        self._connected = True
        self._poll_task = asyncio.create_task(self._poll_loop())
        logger.info("微信平台已连接")
    
    async def disconnect(self) -> None:
        self._connected = False
        if self._poll_task:
            self._poll_task.cancel()
            try:
                await self._poll_task
            except asyncio.CancelledError:
                pass
        if self._client:
            await self._client.close()
        logger.info("微信平台已断开")
    
    async def send_message(self, chat_id: str, content: str) -> bool:
        if not self._client:
            return False
        return await self._client.send_message(chat_id, content)
    
    async def send_typing(self, chat_id: str) -> bool:
        if not self._client:
            return False
        return await self._client.send_typing(chat_id)
    
    def get_name(self) -> str:
        return "wechat"
    
    def is_connected(self) -> bool:
        return self._connected
    
    async def _poll_loop(self) -> None:
        while self._connected:
            try:
                messages = await self._client.get_messages()
                for data in messages:
                    msg = self._convert(data)
                    if msg and self._handler:
                        await self._handler(msg)
                await asyncio.sleep(1)
            except asyncio.CancelledError:
                break
            except Exception as e:
                logger.error(f"轮询出错: {e}")
                await asyncio.sleep(5)
    
    def _convert(self, data: dict) -> Optional[Message]:
        try:
            return Message(
                msg_id=data.get("msg_id", ""),
                platform="wechat",
                user_id=data.get("from_user", ""),
                chat_id=data.get("from_user", ""),
                content=data.get("content", ""),
                msg_type=data.get("type", "text"),
                raw=data,
            )
        except Exception as e:
            logger.error(f"转换消息失败: {e}")
            return None
```

- [ ] **Step 3: 提交**

```bash
git add coloop-agent-channel/src/channel/platform/
git commit -m "feat(channel): 添加微信平台实现"
```

---

## Task 9: CLI 入口

**Files:**
- Create: `coloop-agent-channel/src/channel/cli/__init__.py`
- Create: `coloop-agent-channel/src/channel/cli/app.py`
- Create: `coloop-agent-channel/config.yaml`

- [ ] **Step 1: 实现 CLI 应用**

```python
# src/channel/cli/__init__.py
```

```python
# src/channel/cli/app.py
"""CLI 入口"""

import asyncio
import logging
import signal
import sys
from typing import Optional

import yaml

from ..agent.strategy import AgentStrategy
from ..agent.test import TestStrategy
from ..gateway.consumer import StreamConsumer
from ..gateway.dispatch import StreamDispatch
from ..gateway.registry import PlatformRegistry
from ..platform.wechat.wechat import WeChatPlatform


class ChannelApp:
    """Channel 应用"""
    
    def __init__(self, agent: Optional[AgentStrategy] = None):
        self.agent = agent or TestStrategy()
        self.registry = PlatformRegistry()
        self.consumer = StreamConsumer()
        self.dispatch = StreamDispatch()
        self._running = False
    
    async def start(self, config: dict) -> None:
        self._running = True
        
        # 注册平台
        wechat_config = config.get("wechat", {})
        if wechat_config.get("enabled"):
            platform = WeChatPlatform(
                token=wechat_config["token"],
                api_base=wechat_config.get("api_base", "https://ilinkai.weixin.qq.com"),
            )
            platform.set_message_handler(self.consumer.on_message)
            self.registry.register(platform)
        
        # 启动组件
        await self.consumer.start()
        await self.dispatch.start(self.agent, self.registry, self.consumer)
        await self.registry.connect_all()
        
        logging.info("应用已启动")
        
        # 等待停止
        await self._wait_for_stop()
    
    async def stop(self) -> None:
        self._running = False
        await self.registry.disconnect_all()
        await self.dispatch.stop()
        await self.consumer.stop()
        logging.info("应用已停止")
    
    async def _wait_for_stop(self) -> None:
        stop = asyncio.Event()
        
        def handler():
            stop.set()
        
        loop = asyncio.get_event_loop()
        for sig in (signal.SIGINT, signal.SIGTERM):
            loop.add_signal_handler(sig, handler)
        
        await stop.wait()


def main():
    parser = argparse.ArgumentParser(description="Channel - AI Agent 远程控制")
    parser.add_argument("-c", "--config", default="config.yaml", help="配置文件")
    parser.add_argument("-v", "--verbose", action="store_true", help="详细日志")
    args = parser.parse_args()
    
    logging.basicConfig(
        level=logging.DEBUG if args.verbose else logging.INFO,
        format="%(asctime)s %(name)s %(levelname)s %(message)s",
    )
    
    try:
        with open(args.config) as f:
            config = yaml.safe_load(f)
    except FileNotFoundError:
        config = {}
    
    app = ChannelApp()
    
    try:
        asyncio.run(app.start(config))
    except KeyboardInterrupt:
        asyncio.run(app.stop())


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 创建配置文件**

```yaml
# config.yaml

wechat:
  enabled: false
  token: "your-ilink-token"
  api_base: "https://ilinkai.weixin.qq.com"

agent:
  strategy: "test"
  test:
    delay: 1.0
```

- [ ] **Step 3: 添加 pyproject.toml 入口**

在 pyproject.toml 中添加：

```toml
[project.scripts]
channel = "channel.cli.app:main"
```

- [ ] **Step 4: 提交**

```bash
git add coloop-agent-channel/src/channel/cli/ coloop-agent-channel/config.yaml coloop-agent-channel/pyproject.toml
git commit -m "feat(channel): 添加 CLI 入口"
```

---

## Task 10: 集成测试

**Files:**
- Create: `coloop-agent-channel/tests/test_integration.py`

- [ ] **Step 1: 编写集成测试**

```python
# tests/test_integration.py
"""集成测试"""

import pytest
import asyncio
from unittest.mock import AsyncMock, MagicMock
from channel.core.message import Message
from channel.core.session import Session
from channel.agent.test import TestStrategy
from channel.gateway.consumer import StreamConsumer
from channel.gateway.dispatch import StreamDispatch
from channel.gateway.registry import PlatformRegistry


@pytest.mark.asyncio
async def test_full_flow():
    """测试完整流程"""
    # 创建组件
    agent = TestStrategy(delay=0.01)
    consumer = StreamConsumer()
    dispatch = StreamDispatch()
    registry = PlatformRegistry()
    
    # 模拟平台
    platform = MagicMock()
    platform.get_name.return_value = "test"
    platform.send_message = AsyncMock(return_value=True)
    platform.is_connected.return_value = True
    registry.register(platform)
    
    # 启动组件
    await consumer.start()
    await dispatch.start(agent, registry, consumer)
    
    # 发送消息
    msg = Message(
        msg_id="msg_001",
        platform="test",
        user_id="user_123",
        chat_id="chat_001",
        content="Hello"
    )
    await consumer.on_message(msg)
    
    # 等待处理
    await asyncio.sleep(0.5)
    
    # 验证发送了回复
    assert platform.send_message.call_count >= 3
    
    # 停止
    await dispatch.stop()
    await consumer.stop()


@pytest.mark.asyncio
async def test_message_queuing():
    """测试消息队列"""
    agent = TestStrategy(delay=0.1)
    send_message = AsyncMock()
    
    session = Session(
        chat_id="chat_001",
        agent=agent,
        send_message=send_message,
    )
    
    # 第一条消息
    msg1 = Message(
        msg_id="msg_001",
        platform="test",
        user_id="user_123",
        chat_id="chat_001",
        content="First"
    )
    
    # 开始处理第一条（不等待完成）
    asyncio.create_task(session.on_message(msg1))
    await asyncio.sleep(0.05)
    
    # 第二条消息（应该入队）
    msg2 = Message(
        msg_id="msg_002",
        platform="test",
        user_id="user_123",
        chat_id="chat_001",
        content="Second"
    )
    await session.on_message(msg2)
    
    # 验证第二条消息入队了
    assert not session.queue.empty
```

- [ ] **Step 2: 运行集成测试**

```bash
cd coloop-agent-channel
pytest tests/test_integration.py -v
```

Expected: 2 passed

- [ ] **Step 3: 提交**

```bash
git add coloop-agent-channel/tests/test_integration.py
git commit -m "test(channel): 添加集成测试"
```

---

## 完成

实现计划完成！所有核心组件已定义：

1. ✅ Message - 消息模型
2. ✅ MessageQueue - 消息队列
3. ✅ AgentStrategy - Agent 接口
4. ✅ Session - 会话管理
5. ✅ Platform - 平台接口
6. ✅ StreamConsumer - 消息消费者
7. ✅ StreamDispatch - 消息分发器
8. ✅ WeChatPlatform - 微信平台实现
9. ✅ CLI - 命令行入口
10. ✅ Tests - 测试覆盖
